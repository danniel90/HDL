module Multiplexor4to1;
var
	a, b, c, d: input;
	s[1..0]: output;
	r: output;
begin
	when s == ^b00 then
		r = a;
	else when s == ^b01 then
		r = b;
	else when s ==  ^b10 then
		r = c;
	else
		r = d;
end
