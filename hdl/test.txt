module Multiplexor4to1;
var
	a, b, c, d: input;
	s[1..0]: input;
	r: output;
begin
	when s == ^2b00 then
		r = a;
	else when s == ^2b01 then
		r = b;
	else when s ==  ^2b10 then
		r = c;
	else
		r = d;
end
